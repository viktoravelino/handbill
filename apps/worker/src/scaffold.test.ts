import { expect, test } from "bun:test"

// Placeholder so `bun test` has something to run in this package and CI is green
// from the first push. Replaced by the real Worker tests in M3.
test("worker package is wired into the workspace", () => {
  expect(1).toBe(1)
})
