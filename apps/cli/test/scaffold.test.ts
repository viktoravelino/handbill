import { expect, test } from "bun:test"

// Placeholder so `bun test` has something to run in this package and CI is green
// from the first push. Replaced by the real CLI tests in M4.
test("cli package is wired into the workspace", () => {
  expect(1).toBe(1)
})
