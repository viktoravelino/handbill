import { expect, test } from "bun:test"

// Placeholder so `bun test` has something to run in this package and CI is green
// from the first push. Replaced by the real contract tests in M2 (OpenAPI snapshot).
test("contract package is wired into the workspace", () => {
  expect(1).toBe(1)
})
